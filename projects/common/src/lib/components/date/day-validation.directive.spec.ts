import { DayValidationDirective } from './day-validation.directive';

describe('DateValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new DayValidationDirective();
    expect(directive).toBeTruthy();
  });
});
