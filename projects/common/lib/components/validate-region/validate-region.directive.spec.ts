import { ValidateRegionDirective } from './validate-region.directive';

describe('ValidateRegionDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateRegionDirective();
    expect(directive).toBeTruthy();
  });
});
