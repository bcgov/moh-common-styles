import { ValidateStreetDirective } from './validate-street.directive';

describe('ValidateStreetDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateStreetDirective();
    expect(directive).toBeTruthy();
  });
});
