import { ValidateBcPostalDirective } from './validate-bc-postal.directive';

describe('ValidateBcPostalDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateBcPostalDirective();
    expect(directive).toBeTruthy();
  });
});
