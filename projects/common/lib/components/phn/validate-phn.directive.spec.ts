import { ValidatePhnDirective } from './validate-phn.directive';

describe('ValidatePhnDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidatePhnDirective();
    expect(directive).toBeTruthy();
  });
});
