import { ValidateSinDirective } from './validate-sin.directive';

describe('ValidateSinDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateSinDirective();
    expect(directive).toBeTruthy();
  });
});
