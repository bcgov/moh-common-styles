import { DuplicateSinDirective } from './duplicate-sin.directive';

describe('DuplicateSinDirective', () => {
  it('should create an instance', () => {
    const directive = new DuplicateSinDirective();
    expect(directive).toBeTruthy();
  });
});
