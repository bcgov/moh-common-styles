import { DuplicateCheckDirective } from './duplicate-check.directive';

describe('DuplicateCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new DuplicateCheckDirective();
    expect(directive).toBeTruthy();
  });
});
