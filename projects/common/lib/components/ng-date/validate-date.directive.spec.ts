import { ValidateDateDirective } from './validate-date.directive';

describe('ValidateDateDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateDateDirective();
    expect(directive).toBeTruthy();
  });
});
