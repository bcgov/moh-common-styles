import { YearValidateDirective } from './year-validate.directive';

describe('YearValidateDirective', () => {
  it('should create an instance', () => {
    const directive = new YearValidateDirective();
    expect(directive).toBeTruthy();
  });
});
