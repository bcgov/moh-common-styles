import { DateFieldFormatDirective } from './date-field-format.directive';

describe('DateFieldFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new DateFieldFormatDirective();
    expect(directive).toBeTruthy();
  });
});
