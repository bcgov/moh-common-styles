import { ValidateCityDirective } from './validate-city.directive';

describe('ValidateCityDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateCityDirective();
    expect(directive).toBeTruthy();
  });
});
