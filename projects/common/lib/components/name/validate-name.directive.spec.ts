import { ValidateNameDirective } from './validate-name.directive';

describe('ValidateNameDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateNameDirective();
    expect(directive).toBeTruthy();
  });
});
